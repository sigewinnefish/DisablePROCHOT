name: build

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    

    - name: configure
      working-directory: ${{ github.workspace }}
      run: ./build.sh
